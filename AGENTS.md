# AGENTS RULES

- 現行の割当・報告・独立検収はハレトク統括へ直接集約する．常設の部門レビュワーは廃止し，実装と検収の担当を分離する．横断正本は`../business-research/運営/AI分業.md`とする．

- `business-research`をサービス範囲，ユーザー体験および運営方針の根拠とする．
- `backend/calculation/docs/`を計算式，`backend/data/docs/`をデータ項目，`backend/dist/docs/`を公開成果物の根拠とする．
- 公開リポジトリであるため，秘密情報，APIキー，非公開データおよび内部処理用データを置かない．
- `data/input/`を独自判断で手修正せず，backendの`dist/output/`にある公開成果物を正本とする．
- `qa/fixtures/`はbackendとの一致確認専用とし，公開サイトの実行時データとして使用しない．
- 未確定の計算式または未検証のデータから，採算値を生成しない．
- 得にならない結果もそのまま表示し，採算が良く見えるように前提を変更しない．
- 結論を最初に示し，内訳と前提・詳細情報は段階的開示とする．外部見積もり導線は，トップでは診断開始より弱い副CTAとして1か所，診断では主要結果の直後に1か所，記事では内容から必要性が生じる箇所だけに置く．
- 見積もり導線には，広告・アフィリエイトであることと，住宅固有条件の確定に見積もりが必要な理由を表示する．
- 計算処理または計算に関係するデータ連携を変更する場合は，frontend QAを実行し，backendの基準計算ケースとの一致を確認する．
- 見た目，レイアウトまたは導線を変更した後は，自動テストだけで完了とせず，実ブラウザで確認する．
- 通常のfrontend開発は`pnpm run dev`によるローカル確認で進め，変更のたびにビルドまたはデプロイしない．
- デプロイはユーザーの明示承認がある場合に限る．公開可能という検収判定だけで公開操作の承認としない．
- 同一の画面開発を継続している間は，ローカル開発サーバーを停止せず，`node_modules/`を削除しない．
- 人間が判断したUX，文章，情報の優先順位および見た目の方針は，`site/docs/画面設計.md`へ反映する．
- 必要性が確定するまで，フレームワーク，サーバー，データベースおよび外部解析サービスを追加しない．
- HTMLは意味構造，フォームラベル，キーボード操作および画面幅の違いに配慮する．
- 各仕様文書では，各`##`の直下に`> **結論：〜．**`の形式で一文の結論を置く．
- 日本語文書の句読点には「，」「．」を用いる．
- ファイルの保持はREADMEの保持方針に従う．作業ログ，検収・引渡し記録，確認画像，旧案および不要な生成物を恒久保存しない．必要な決定・制約は既存の正本文書へ反映する．
- 一時ファイルは可能な限りOSの一時領域へ作成し，確認・引渡し後に削除する．Gitの無視対象も清掃対象とする．稼働中の処理が使うファイルは使用終了後に削除する．

# 実装方針

## 基本方針

> **結論：MVPは，静的サイトとしてブラウザ内の計算と事前生成データで完結させる．**

サイト本体にはHTML，CSSおよび依存ライブラリを持たないJavaScript ES Modulesを使用する．確認用公開と複数HTMLの成果物生成に限ってViteおよびCloudflare Workers互換設定を使用するが，実行時のサーバー処理，データベースおよびクラウド関数は使用しない．

## JavaScriptの分担

> **結論：画面制御，公開データ読込および採算計算を分離する．**

| ファイル | 役割 |
|---|---|
| `assets/js/app.js` | 初期化，フォーム制御および各処理の接続 |
| `assets/js/data-loader.js` | 公開データの取得と版の整合性確認 |
| `assets/js/calculator.js` | backend仕様と一致するブラウザ採算計算 |

都道府県選択肢は`data/public-data.json`から生成し，frontend内に同じ一覧を重複して保持しない．

## backend連携

> **結論：frontendは，backendが生成した3種類のJSONを`data/`から読み込む．**

| ファイル | 用途 |
|---|---|
| `data/public-data.json` | ブラウザ計算に必要な公開データ |
| `data/metadata.json` | スキーマ版，データ版，生成日時および出典概要 |
| `data/calculation-cases.json` | frontend計算とbackend基準計算の一致確認 |

公開データとメタデータの`schema_version`および`data_version`が一致しない場合は，計算機能を有効にしない．秘密情報またはbackendの内部データへ直接接続しない．

## 利用可否

> **結論：公開データの読込または版の整合性確認に失敗した場合，計算ボタンを無効にする．**

`data_version`が`uninitialized`である場合，公開データとメタデータの版が一致しない場合，または計算処理が未実装の場合は，利用できない理由を画面に表示する．

## テスト

> **結論：計算，公開データ，HTML構造および内部リンクを，依存ライブラリを追加せずNode.js標準テストで確認する．**

47都道府県のbackend基準計算との一致，上振れ・標準・下振れ，補助金を含めないケース，境界値，不正入力，HTMLの意味構造，見積もり表示および内部リンクを確認する．`pnpm test`で実行し，計算式と丸め規則はbackendの計算仕様を正とする．
